package starter.multiAI.Actions

import screeps.api.Creep
import starter.multiAI.MultiAI

object HaulerBase {
	fun
			haulerBase (creep: Creep): MultiAI.ReturnType{
		//if energy == 0 get energy
		//fill up nearest extention
		//then towers
		//then upgraders
		return MultiAI.ReturnType.CONTINUE
	}
}