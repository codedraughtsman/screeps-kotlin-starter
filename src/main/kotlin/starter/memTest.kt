package starter

import screeps.utils.memory.MemoryDelegate

/*
private external interface Person {
	var name: String?
	var age: Int // non nullable type here is dangerous because we could forget initialization
}

private external interface Kangaroo {
	var belly: Person?
	var name: String
	var friends: Array<Person>?
}


private external interface memTest {
	var xVar: Int?

}
class memTestReal (xValue: Int): memTest{
	init {
		xVar =xValue
	}

	override var xVar: Int?
		get() = TODO("not implemented") //To change initializer of created properties use File | Settings | File Templates.
		set(value) {}

}
*/
